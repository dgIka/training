version: '3.8'

services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ImodiumExpress1
      POSTGRES_DB: javarush_training_db
    ports:
      - "5432:5432"
    volumes:
          - ./pgdata:/var/lib/postgresql/data/